# 캐릭터 이미지 제작 규칙

이 저장소는 스마트올 캐릭터 4종의 2D/3D 이미지 일관성을 관리한다.

## 작업 순서

1. 캐릭터가 등장하면 `characters/2d` 또는 `characters/3d`의 해당 Character Master를 먼저 읽는다.
2. `style`의 공통 색상과 스타일 규칙을 적용한다.
3. `templates`의 프롬프트 구조를 사용한다.
4. 사용자가 확정한 결과만 `prompts`에 기록한다.
5. 수정 피드백에서 반복 가능한 규칙을 발견하면 해당 Character Master나 스타일 문서에 반영한다.

## 공통 원칙

- 캐릭터의 실루엣, 색상, 얼굴 배치와 시그니처 요소를 임의로 바꾸지 않는다.
- 2D와 3D 규칙을 섞지 않는다.
- 레퍼런스 사진은 사용자가 지정한 항목만 참고한다.
- 확정 전 결과를 기준 자산으로 취급하지 않는다.
